{% extends 'base.html.twig' %}

{% block body %}
    <div class="example-wrapper">
        <h1>Venez faire le plein de tricks</h1>
        <section class="trick-List">
            {% for trick in tricks %}
                <div class="content">
                    <div>
                        <img src="https://placehold.co/350x300" alt="">
                    </div>
                    <div>
                        <a href="" class="">{{ trick.name }}</a>
                        <a href="" class="btn btn-primary"><i class="fas fa-edit"></i></a>
                        <a href="" class="btn btn-primary"><i class="fas fa-trash-alt"></i></a>
                    </div>
                </div>
            {% endfor %}
        </section>
    </div>
{% endblock %}

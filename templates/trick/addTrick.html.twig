{% extends 'layout.html.twig' %}

{% block container %}
    <!-- BANNER SECTION -->
    <section class="add-trick-banner"></section>
    <!-- ADD TRICK SECTION -->
    <div class="add-trick">
        {{ form_start(trickForm) }}
            {{ include("trick/_form.html.twig", {form: trickForm}) }}
            <div class="btn-add">
                <button class="btn btn-primary btn-trick">Ajouter la figure</button>
            </div>
        {{ form_end(trickForm, {render_rest: false}) }}
    </div>
{% endblock %}